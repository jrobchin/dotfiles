export PROMPT="%~ $ "
export RPROMPT="%t"

export EDITOR="nvim"

# ALIASES
alias v="nvim ."

alias sz="source ~/.zshrc"

alias czsh="cd && nvim ~/.zshrc && cd -"
alias cnvim="cd ~/.config/nvim && nvim . && cd -"
alias ckitty="cd ~/.config/kitty && nvim . && cd -"

# SHELL INTEGRATIONS
source <(fzf --zsh)
if [[ "$CLAUDECODE" != "1" ]]; then
    eval "$(zoxide init --cmd cd zsh)"
fi

# Fix for ALT-C for fzf
bindkey "ç" fzf-cd-widget

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"

eval "$(/opt/homebrew/bin/brew shellenv)"

autoload -Uz vcs_info
setopt prompt_subst

precmd() { vcs_info }
zstyle ':vcs_info:git*' formats '%F{yellow}(%b)%f'

prompt_dir_with_slash() {
  local dir
  dir=$(print -P "%1~")
  if [[ $dir == "/" ]]; then
    printf "/"
  else
    printf "%s/" "${dir%/}"
  fi
}

PROMPT='%F{cyan}$(prompt_dir_with_slash)%f${vcs_info_msg_0_:+ ${vcs_info_msg_0_}} %# '
RPROMPT='%F{green}%t%f'
